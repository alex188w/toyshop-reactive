spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/toyshop
      client:
        registration:
          keycloak:
            client-id: storefront
            client-secret: yvwKiveaN9tAXbpBmqNZa4155A2KHAXM
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope: openid, profile, email
        provider:
          keycloak:
            # ⚠️ для продакшена/докера можно вернуть https://keycloak.example.com
            issuer-uri: http://localhost:8080/realms/toyshop

  webflux:
    static-path-pattern: "/uploads/**"

  application:
    name: toyshop

  sql:
    init:
      mode: never   # для R2DBC jdbc init не работает

  data:
    r2dbc:
      repositories:
        enabled: true
    redis:
      database: 0

server:
  port: 8085



